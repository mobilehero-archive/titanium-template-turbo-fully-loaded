{
	"name": "${name}",
	"version": "1.0.0-0",
	"description": "${description}",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"lint": "eslint ./app",		
		"prebuild": "ti sdk install",	
		"clean": "rm -rfv build && rm -rfv i18n && rm -rfv platform && rm -rfv Resources",		
		"build": "npm run build:ios && npm run build:android",
		"build:ios": "ti build -p ios --build-only --log-level trace --ios-version 13.2.2",
		"build:android": "ti build -p android --build-only --log-level trace"
	},
	"license": "${license}",
	"dependencies": {
		"@titanium/bluebird": ">=3.7.2",
		"@titanium/polyfill": ">=0.0.6",
		"@titanium/errors": ">=0.0.2",
		"@titanium/screenshot": "latest",
		"@titanium/applesignin": "latest",
		"@titanium/googlesignin": "latest",
		"@titanium/facebook": ">=1.0.10",
		"@titanium/lottie": ">=1.0.17",
		"@titanium/permissions": ">=0.0.6",
		"@titanium/webdialog": "^1.0.13",
		"@titanium/identity": ">=1.1.0",
		"@titanium/please": ">=0.0.10",
		"@titanium/streams": ">=0.0.3",
		"fast-json-patch": "^3.0.0-1",
		"semver": "^7.3.2"
		
	},
	"devDependencies": {
		"lodash": ">=4.17.15",
		"@titanium/eslint-config": ">=0.0.11",
		"@titanium/plugin-turbo": ">=1.0.4",
		"@titanium/turbo": "latest",
		"eslint": "^7.0.0",
		"titanium": ">=5.2.2"		
	},
	"author": {
		"name": "${author_name}",
		"email": "${author_email}",
		"url": "${author_url}"
	},
	"repository": {
		"type": "${repo_type}",
		"url": "${repo_url}"
	},
	"bugs": {
		"url": "${bugs_url}",
		"email": "${bugs_email}"
	},
	"keywords": ["titanium", "mobile", "alloy", "turbo", "appcelerator", "axway", "amplify"]
}
